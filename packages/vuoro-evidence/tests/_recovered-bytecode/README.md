# Recovered test bytecode

The only surviving artifact of this package's test suite (see `../../RECOVERY.md`).
Pytest-assertion-rewritten CPython 3.12 bytecode, moved out of `__pycache__/` so
it is tracked and survives a cache sweep.  Not importable as a test suite; kept
so the assertions can still be read back.
